// TODO: make a hazard detection / forwarding unit
    // read after write forwarding
    // load use hazard (single stall, using write enable and reset signals on pipeline regs)
    // taken branches (flush, using reset signals on pipeline regs)
// Do some research... you might need some registers to store signals from previous instructions or smth